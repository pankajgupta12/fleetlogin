<?php 

	/*	include('include/header.php');
		$shift = new Shift;
        if(isset($_GET['task']) && $_GET['task'] == '') {
			$shift->logincheck();
		   include('pre_shift.php');
	    } else if(isset($_GET['task']) && $_GET['task'] == 'pre_shift') {
	        
	       // print_r($_SESSION);  die;
	        
		//	$shift->logincheck();
			include('pre_shift.php');
	    }else if(isset($_GET['task']) && $_GET['task'] == 'post_shift') {
		//	$shift->logincheck();
			include('post_shift.php');
	    }else  if(isset($_GET['task']) && $_GET['task'] == 'logout') {
		   $shift->logout();
	    }else{
			include('not_found.php');
		}*/
 ?>
</body>
</html>